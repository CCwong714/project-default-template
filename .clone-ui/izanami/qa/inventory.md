# Izanami QA inventory

## Verified visual states

- Desktop hero and section captures at 1920×873; exact total height: 11,510px.
- Mobile hero, menu, footer, and eight representative scroll captures at 390×844; no horizontal overflow.
- Full-page structure: Philosophy collage/copy, Projects intro, School/Craft/Retreat panels, Company, and Footer.
- Mobile footer order matches the source: navigation → social/privacy links → locations → clocks/page top.

## Verified interactions

- Initial load and refresh show the source-matched black loader: its title enters character-by-character, the lower-left value advances from 0 to 100, then the hero and global controls are revealed through the three-second loader fade.
- Menu closed → opening → settled open → closed. At 520ms the overlay was already at 0.978 opacity; at settlement all five main links and the subnav were fully opaque and unblurred.
- Rapid reversal: opening was interrupted at 120ms, then closed; overlay, page scale, body lock, and `aria-expanded` all returned to the closed state.
- Header JA hover now preserves the source's empty circle row, rolls paired text from `0/-110%` and `130/0%`, and grows a full-width line from the left over 700ms. MENU vertically exchanges paired dots over 700ms, rolls paired text by the same offsets, and keeps a fixed width while MENU/CLOSE crossfade over 400ms. Enter, settled, interrupted leave, click-state, and `:focus-visible` selectors were checked at 1918×928. Interrupted leave kills the active tween and begins a fresh full-duration 700ms `quart.out` return, matching the source instead of CSS transition reversal shortening.
- CTA hover now uses the source's separate button runtime: a single full-button 1px line translates from a measured idle offset through the text while paired labels exchange at `0/-130%` and `130/0%`. Desktop enter/settled/leave/rapid-reversal states were sampled at 1470×746; enter and text return take 700ms, the line return takes 1.1s, and all use `quart.out`. Keyboard focus runs the same animation with the existing visible outline, and font completion triggers a final cold-load geometry measurement. The mobile layout keeps its exact 19.9004975124vw leading track and 4.9751243781vw gutters with zero horizontal overflow.
- The shared hover primitives continue to cover primary nav, subnav, CTA buttons, footer navigation, social links, next-page, and page-top controls.
- Pointer motion produces an advected GPU velocity-and-dye field that displaces the visible page imagery into the source-matched liquid folds. The field decays to a transparent canvas after input stops and the render loop then sleeps.
- The black School, Craft, and Retreat sequence is now pointer-silent. At 1470×802 the canvas remains active before the stack, becomes hidden and fully cleared at School (y=5000), Craft (y=5600), and Retreat (y=6200), then returns cleanly after the stack at y=6800. A partial-viewport boundary check at y=4600 confirmed that a pointer above the stack keeps the effect active, crossing below its measured 199.25px edge disables it immediately, and returning above restores it. A final 1920×929 regression at a 199.91px boundary also confirmed enter/exit state and the rebased first outside event. Mobile 390×844 remains on its existing no-fluid path with zero horizontal overflow.
- The supplied 1920×928 Footer recording was sampled at 25 states from 0–11.744s. Company/IZANAMI and the complete Footer now join the School/Craft/Retreat stack as fluid-disabled regions; real pointer movement at the center of Company and inside Footer leaves the cleared WebGL canvas hidden. Three disabled regions are registered, desktop remains overflow-free with no runtime overlay, and mobile keeps the existing `display: none` fluid fallback.
- Footer navigation, social/privacy, and TOP links now match the source regular-link runtime: original `0 → -110%`, clone `130% → 0`, and line `scaleX(0 → 1)` over 0.7s `quart.out`, with in-flight tweens killed before every reversal. WhatsApp and Instagram now use the source 16×16 paths and keep their icons static while the label rolls.
- Footer social/privacy geometry was independently remeasured at 1280×720 after the user-reported drift. The source and clone now both render whatsapp/Instagram/privacy policy clips at 64.609375/72.0703125/100.25px wide and 15.34375px high with `overflow: clip`, `padding-block: 1.28px`, and the strike line centered in the same grid cell. At 180ms both produce text transforms `-9.88798px/4.95014px` and line scale `0.7024`; settled transforms and all three full line widths also match. A 100ms interrupted enter followed by leave returns to the exact idle matrices. Evidence: `source/footer-link-hover-states-20260813.json`.
- Fresh adversarial Pass D found no blocking hover drift: grid overlap, authored casing, 0.7s `quart.out` motion, rapid reversal, static icons, mobile order, coarse-pointer/reduced-motion fallbacks, focus behavior, cleanup, and nested-ternary policy all passed. The remaining `/privacy/` versus local `#contact` destination is a separate unimplemented route, not part of this hover correction.
- The current Footer HOME item keeps the source's visible page dot but is removed from pointer and keyboard interaction, so it cannot replay a hover or navigate to the already-current page.
- The large Footer Philosophy link now matches the separate button runtime and entry observer. At a 1389px viewport its 1250.09375px line measures `x=-937.491px` idle and `x=312.509px` active versus source `-937.483px/312.517px`; text exchanges by 130%, enter is 0.7s, line leave is 1.1s, and its initial line sweep/1.2s staggered character reveal matches the supplied recording. The link width is the source-sized 554.515625px rather than filling the Footer.
- Desktop 1920×928 and mobile 390×844 Footer receipts confirm no horizontal overflow. On mobile the Footer foot ends inside the fixed 198.6177884615vw container; reduced-motion shows the final readable title and short idle line while the temporary entry sweep line is fully clipped.
- The hero title/background, collage images, text groups, project panels, and company mark were checked through representative scroll positions.
- The supplied 1918×928 Company/Footer recording was sampled every 250ms across its full 5.368s. The erroneous desktop `position: sticky` pause was removed: the live source moves Company content -321px for a 321px scroll, and the clone now does the same instead of holding at 0px. A matched-duration 1470×802 run produced 323 frames, 17.4ms P95, no frame over 20ms, and no long task; Company copy/logo compute to `filter: none`. The 1918×928 handoff/mid/Footer captures and the 390×844 regression captures are under `output/playwright/runtime-audit/`; numerical evidence is `source/company-footer-scroll-20260814.json`.
- Desktop scroll geometry now matches the live source at 1918×928: 11,499px total height, a 6,262px Projects/School handoff, and the same 6,262px result for an identical wheel input.
- The Projects background now behaves as the source's unclipped desktop mesh: while the Philosophy collage enters, the architecture is already visible at the source 0.8 parallax position instead of leaving a black gap.
- The architecture remains visible beneath the entering School foreground only during the source's bottom-driven fade, then reaches black exactly as the background target bottom reaches the viewport top; the three desktop sticky panels retain the source natural heights, compression, and intermediate sticky top.
- The supplied 2940×1602 recording was sampled at 24 frames. Desktop comparisons at 1470×802 covered y=900, 1200, 1800, 2200, 2800, 3351, 3900, 4200, 4500, and 4800; the corrected background top at y=1800 is 305.25px versus the source formula's 310.25px, with no horizontal overflow or console warnings/errors.
- Mobile 390×844 preserves the source's non-post-effect path: no early architecture bleed at y=800, then the image begins at the measured ~2652px document position and follows the existing fade/handoff.
- Mobile geometry at 390×844 now matches the live source at 7,620px total height, including exact Hero, Philosophy, Projects intro/background, all three project panels, Company, and Footer boundaries.
- The supplied 1458×1602 mobile recording was sampled at 36 frames and reproduced at its 729×801 CSS viewport. Hero forest/black handoff, Philosophy, Projects, School, Craft, Retreat, Company, and Footer were compared at the recording's observed 0.683s–5.367s section transitions.
- At 729×801, source and clone both measure 14,243px high. The corrected Company background is exactly 1,276.65625px high; its first paragraph and CTA heights are exact, while their measured document tops differ by about 0.98px.
- The same mobile path was regression-checked at 390×844: 7,620px total height, 390px scroll width, no horizontal overflow, and the Company image fades before the longer copy section hands off to Footer.
- The Company/Footer correction preserves those exact mobile dimensions: Company remains 820.84375px, its shorter background remains 682.984375px, and content/section tops both move -160px and -320px for the same scroll offsets before the 774.609375px Footer.
- Hero colour was rechecked at 1918×930 against the supplied 2026-08-12 reference and the live source. The source and local Hero assets have an identical SHA-256; the clone's extra 2%-to-22% black gradient was removed, cloud opacity was restored from 0.43 to the source's 1, and effective cloud tops now match the source at -18.375vw/0 on desktop and 34.8258706468vw/54.7263681592vw on mobile. Top/middle/bottom luma improved from 141.45/114.7/82.75 to 168.19/162.9/136.54 versus 162.35/153.64/141.02 in the supplied reference. Desktop and 390×844 mobile both retain exact viewport width with no console warnings/errors.
- The first-screen clock/copyright/Scroll strip becomes hidden after the source-matched orientation-aware scroll threshold.

## Fallbacks

- Coarse pointers hide the header hover strike lines, and viewports at or below 767px do not instantiate the desktop water trail; changing those media conditions also starts or disposes the renderer dynamically.
- Opening the navigation suspends Lenis; desktop coarse pointers use the source's non-sticky project fallback instead of inheriting fine-pointer sticky offsets.
- Reduced-motion changes are observed live and disable the trail, Lenis smoothing, GSAP scrub timelines, cloud animation, and long transitions.

Screenshots and source comparisons are stored beside this file and under `../source/captures/`. The final Hero colour captures are `local-desktop-hero-color-20260812.jpg` at 1918×930 and `local-mobile-hero-color-20260812.jpg` at 390×844. CTA idle and active receipts are `local-desktop-button-hover-idle-20260812.jpg` and `local-desktop-button-hover-active-20260812.jpg`. The pointer-silent project receipt is `local-desktop-projects-fluid-disabled-20260812.jpg`. Final Footer receipts are `output/playwright/izanami-footer-bottom-1920x928.png` and `output/playwright/izanami-footer-bottom-390x844-final.png`.

## EN/JA route QA — 2026-08-13

- Claim: clicking JA follows the source route `/ja/`, marks JA as current, updates the document language to `ja`, and replaces only the body copy that the source translates. Check by activating the visible JA control, then compare Philosophy, Projects intro, School/Craft/Retreat, and Company against `source/raw-ja.html`.
- Claim: source-retained English remains unchanged in JA mode: Hero title, section headings, project names, CTA labels, menu/footer navigation, location names, and clocks. Check the initial viewport, menu, each translated region, and Footer.
- Claim: clicking EN completes the full reverse cycle to `/`, restores `lang="en"`, marks EN current, and restores the original English copy.
- Visual desktop state: 1470×802 settled JA route, with focused captures of Philosophy, all project copy, and the densest three-paragraph Company block; inspect line breaks, font rendering, CTA placement, clipping, and horizontal overflow.
- Visual mobile state: 390×844 settled JA route at the same translated regions; inspect the longer Japanese copy within fixed-height mobile sections and confirm no clipping or horizontal overflow.
- Exploratory checks verified: `/ja` without the trailing slash and a direct refresh both preserve `lang="ja"` and Japanese copy; browser Back returns to the Japanese route; an unrelated path still renders the existing 404 page.
- Evidence: `output/playwright/izanami-ja-*.png`, text/geometry snapshots, console error capture, and desktop/mobile overflow metrics.
- Result: Codex in-app Browser clicked JA from `/`, reached `/ja/`, reported `lang="ja"`, JA `aria-current="page"`, Shippori Mincho, and exact source Japanese copy; it then clicked EN, returned to `/`, restored `lang="en"`, EN current state, two English Company paragraphs, and the English project copy.
- Desktop 1470×802: the three Japanese Company paragraphs measured 70.5625/141.125/105.84375px, matching the live source; the complete 532.5px copy block and CTA remained inside the viewport, and `scrollWidth === clientWidth === 1470`.
- Mobile 390×844: the source-equivalent mobile continuation break renders the first Company paragraph as three intentional lines, the 465.34375px copy block and CTA remain visible, SCROLL measures as one 40.63×10.71px line, and `scrollWidth === clientWidth === 390` across the full 7,620px page.
- Runtime logs: zero local browser warnings/errors. Focused local captures are `output/playwright/izanami-ja-hero-1470x802.png`, `izanami-ja-philosophy-1470x802.png`, `izanami-ja-projects-1470x802.png`, `izanami-ja-school-1470x802.png`, `izanami-ja-company-1470x802.png`, `izanami-ja-hero-390x844.png`, `izanami-ja-philosophy-390x844.png`, `izanami-ja-projects-390x664.png`, and `izanami-ja-company-390x664.png`; source mobile comparison is `source/captures/source-ja-philosophy-390x844.png`.
